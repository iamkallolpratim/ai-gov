# Base URL of the FastAPI backend as seen from the *browser*.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Base URL as seen from the Next.js server (route handlers). Only differs when the
# client runs inside docker compose, where the API is reachable as http://api:8000.
INTERNAL_API_URL=http://localhost:8000
